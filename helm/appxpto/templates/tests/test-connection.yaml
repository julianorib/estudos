apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appxpto.fullname" . }}-test-connection"
  labels:
    {{- include "appxpto.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appxpto.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
