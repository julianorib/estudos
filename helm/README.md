# Criando templates do Helm

```
helm create appxpto
```

```
helm install myapp .\caminho --dry-run --debug > teste.yaml
```

```
helm install myapp .caminho --set nginx.version=2.3.4 --dry-run --debug > teste.yaml
```


